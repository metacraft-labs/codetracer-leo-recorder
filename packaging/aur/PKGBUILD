# Maintainer: Metacraft Labs <ci@metacraft-labs.com>
#
# Generated by codetracer-launcher/packaging/recorder-templates/generate.py
# from codetracer-leo-recorder/packaging/recorder-metadata.yml.
# DO NOT edit this file by hand — re-run the generator instead.
#
# PKGBUILD for codetracer-leo-recorder (CodeTracer recorder for Leo zero-knowledge programs (Aleo)).

pkgname=codetracer-leo-recorder
pkgver=0.1.0
pkgrel=1
pkgdesc="CodeTracer recorder for Leo zero-knowledge programs (Aleo)"
arch=('x86_64' 'aarch64')
url="https://github.com/metacraft-labs/codetracer-leo-recorder"
license=('Apache-2.0')
depends=('glibc' 'zstd')
makedepends=('rust' 'cargo')
provides=("codetracer-leo-recorder")
source=("$pkgname-$pkgver.tar.gz::https://github.com/metacraft-labs/codetracer-leo-recorder/archive/refs/tags/v$pkgver.tar.gz")
sha256sums=('REPLACE_AT_PUBLISH_TIME_source')

build() {
  cd "$srcdir/codetracer-leo-recorder-$pkgver"
  cargo build --release --locked --bin codetracer-leo-recorder
}

check() {
  cd "$srcdir/codetracer-leo-recorder-$pkgver"
  cargo test --release --locked --bin codetracer-leo-recorder || true
}

package() {
  cd "$srcdir/codetracer-leo-recorder-$pkgver"
  install -Dm755 "target/release/codetracer-leo-recorder" \
    "$pkgdir/usr/bin/codetracer-leo-recorder"
  if [[ -f LICENSE ]]; then
    install -Dm644 LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
  fi
  if [[ -f README.md ]]; then
    install -Dm644 README.md "$pkgdir/usr/share/doc/$pkgname/README.md"
  fi
}
